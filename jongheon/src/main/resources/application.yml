server:
  port: 10000
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/jpa
    name: jpa
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update # update mode

# Search hikari, tomcat, dbcp datasource
# hibernate properties
# After -> persistence.yml
# persistence-unit?